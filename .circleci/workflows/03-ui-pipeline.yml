# ── 03. UI Front-End Pipeline ──────────────────────────────────────────
# Criticality: P1
# Duration: ~30s (with 4x Vitest shards)
# Scope: TypeScript typecheck, ESLint, Timezone invariance, Vitest unit tests

jobs:
  ui-lint-typecheck:
    executor: node-executor
    steps:
      - checkout
      - run:
          name: Install Python (Timezone Invariance)
          command: |
            sudo apt-get update
            sudo apt-get install -y --no-install-recommends python3
          no_output_timeout: 15m
      - restore_cache:
          keys:
            - v2-ui-deps-{{ checksum "ui/package-lock.json" }}
            - v2-ui-deps-
      - run:
          name: Install UI Dependencies
          command: cd ui && npm ci --no-audit --no-fund --ignore-scripts
          no_output_timeout: 15m
      - save_cache:
          key: v2-ui-deps-{{ checksum "ui/package-lock.json" }}
          paths:
            - ~/.npm
            - ui/node_modules
      - run:
          name: UI Typecheck
          command: cd ui && npm run typecheck
          no_output_timeout: 15m
      - run:
          name: UI Lint
          command: cd ui && npm run lint
          no_output_timeout: 15m
      - run:
          name: Analytics Timezone Invariance
          command: python3 scripts/check-tz-invariance.py
          no_output_timeout: 15m

  ui-test-slice:
    executor: node-executor
    parameters:
      slice:
        type: integer
      total:
        type: integer
        default: 4
    steps:
      - checkout
      - restore_cache:
          keys:
            - v2-ui-deps-{{ checksum "ui/package-lock.json" }}
            - v2-ui-deps-
      - run:
          name: Install UI Dependencies
          command: cd ui && npm ci --no-audit --no-fund --ignore-scripts
          no_output_timeout: 15m
      - save_cache:
          key: v2-ui-deps-{{ checksum "ui/package-lock.json" }}
          paths:
            - ~/.npm
            - ui/node_modules
      - run:
          name: Run Vitest Shard << parameters.slice >>/<< parameters.total >>
          command: cd ui && npx vitest run --shard=<< parameters.slice >>/<< parameters.total >>
          no_output_timeout: 15m
