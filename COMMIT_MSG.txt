feat(ui): POS sales screen — side-by-side product lookup + cart panel

PosScreen.tsx + .css (new):
- Split-panel layout: ProductLookupScreen (left, flex 1) + cart panel (right, 380px)
- Cart panel: header with line count badge, scrollable line items, footer with subtotal + Pay button
- Each line: product name, SKU, unit price, qty controls (-/+/value), line total, remove button
- Qty buttons disabled at 1 (decrement); Pay button hidden when cart empty
- SVG icons: Trash, Minus, Plus (all aria-hidden)

usePosState.ts (new):
- Custom hook: lines state, addProduct (increments qty for duplicate SKUs),
  removeLine, updateQty (min 1), memoized total

Updated files:
- domain.ts: added name field to CartLine (optional, for display)
- App.tsx: 'sales' route renders PosScreen (replaces CartScreen)
- en-US.ftl: added 4 POS screen Fluent strings
- main.tsx: registered new Fluent strings
- CartScreen.test.tsx: added name to test CartLine

TypeScript 0 errors, 64 tests pass, ESLint 0 errors.
