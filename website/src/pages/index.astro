---
// Root — locale auto-detect (website-plan.md §4). Static page, so detection
// is client-side: browser language wins; noscript falls back to /en/.
// (CF-IPCountry sniffing would require a Cloudflare Function — out of v1.)
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>OZ-POS</title>
    <script is:inline>
      // Privacy-friendly: browser language only, never geo-tracking.
      const lang = (navigator.language || 'en').toLowerCase();
      const target = lang.startsWith('id') ? '/id/' : '/en/';
      window.location.replace(target);
    </script>
    <noscript>
      <meta http-equiv="refresh" content="0; url=/en/" />
    </noscript>
  </head>
  <body></body>
</html>
