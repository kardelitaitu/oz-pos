---
import Base from '../layouts/Base.astro';
import { t } from '../i18n';

// The root 404 is locale-agnostic; default to EN and offer both locales.
const locale = 'en';
---

<Base title="404 — OZ-POS">
  <section class="mx-auto max-w-6xl px-4 py-24 text-center">
    <p class="text-7xl font-bold text-accent">404</p>
    <h1 class="mt-4 text-2xl font-semibold">{t(locale, 'notFound.title')}</h1>
    <p class="mt-2 text-muted">{t(locale, 'notFound.subtitle')}</p>
    <div class="mt-8 flex flex-col items-center justify-center gap-3 sm:flex-row">
      <a
        href="/en/"
        class="rounded-md bg-accent px-5 py-2.5 text-sm font-semibold text-black transition hover:opacity-90"
      >
        {t(locale, 'notFound.home')}
      </a>
      <a href="/id/" class="rounded-md border border-ink/15 px-5 py-2.5 text-sm font-semibold text-ink transition hover:bg-ink/5">
        Bahasa Indonesia
      </a>
    </div>
  </section>
</Base>
