# Cloudflare Pages redirects (website-plan.md §8, §10).
#
# NOTE: /en/docs/ and /id/docs/ are now REAL pages (the 4-card docs hub —
# src/pages/[locale]/docs/index.astro) — no redirect needed. Keep this file
# minimal; add new redirects here as the site grows.
