# OZ-POS Performance Baselines — 2026-07-21

> Generated via `cargo bench -p oz-core` (Criterion.rs, default sample count).
> All times in **nanoseconds** (ns) unless otherwise noted. Lower is better.

## Hardware

- **CI runner:** GitHub Actions `ubuntu-latest` (4 vCPU, 16 GB RAM, SSD)
- **Local reference:** Intel Core i7-13700K, 32 GB DDR5, Windows 11

## Benchmarks

### Barcode Lookup — `get_product()`

| Benchmark | Time (ns) | vs. Baseline | Notes |
|-----------|-----------|-------------|-------|
| `barcode_lookup_1000_products` | — | — | *Run `cargo bench -p oz-core -- barcode_lookup` to populate* |
| `barcode_lookup_cache_hit` | — | — | *Run to populate* |
| `barcode_lookup_miss` | — | — | *Run to populate* |

### Cart Operations

| Benchmark | Time (ns) | vs. Baseline | Notes |
|-----------|-----------|-------------|-------|
| `cart_add_line` | — | — | *Run `cargo bench -p oz-core -- cart_bench` to populate* |
| `cart_total_20_items` | — | — | *Run to populate* |

### Money Arithmetic

| Benchmark | Time (ns) | vs. Baseline | Notes |
|-----------|-----------|-------------|-------|
| `money_checked_add` | — | — | *Run `cargo bench -p oz-core -- money_bench` to populate* |
| `money_checked_sub` | — | — | *Run to populate* |
| `money_checked_mul` | — | — | *Run to populate* |
| `money_checked_div` | — | — | *Run to populate* |
| `money_serde_roundtrip` | — | — | *Run to populate* |

### Transaction Commit — `create_sale()`

| Benchmark | Time (ns) | vs. Baseline | Notes |
|-----------|-----------|-------------|-------|
| `create_sale_minimal` | — | — | *Run `cargo bench -p oz-core -- transaction_commit` to populate* |
| `create_sale_with_5_lines` | — | — | *Run to populate* |
| `complete_checkout_5_items` | — | — | *Run to populate* |

## Regeneration

```bash
# Run all benchmarks
cargo bench -p oz-core

# Update this file with the output from:
#   grep -E "time:\s*\[|bench" target/criterion/benchmark-output.txt
```
