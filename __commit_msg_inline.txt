fix(test): add api/products mock so ProductLookupScreen tests use SAMPLE_PRODUCTS fallback

All 14 pre-existing test failures were caused by a data mismatch:
the global dev-mock (tauri-api.ts) returns PC hardware products (AMD
Ryzen, RTX 4070) when listProducts() IPC succeeds, but the test expects
coffee-shop items (Caffè Latte, Espresso Shot) from useProducts.ts's
SAMPLE_PRODUCTS fallback.

Added vi.mock('@/api/products') with importOriginal + listProducts /
listCategories → rejects, forcing useProducts into the catch path that
sets SAMPLE_PRODUCTS + SAMPLE_CATEGORY_META. All other exports remain
the real implementations.

22/22 tests pass (up from 8/22).
