fix(db): audit findings — migration ordering, foreign_keys PRAGMA, orphaned files

Database layer audit (0.0.23):

1. Fix migration ordering: moved `046_track_serial.sql` from between 047
   entries to before all 047 entries (correct semantic order — all 046
   migrations now run before 047 starts).

2. Add `PRAGMA foreign_keys = ON` to `run()` startup function — SQLite
   defaults foreign_keys OFF. Previously only 4 of 24+ DB modules set
   it manually; now every connection through `run()` enforces FK
   constraints.

3. Remove 2 orphaned migration files: `034_active_carts.sql` (superseded
   by 037_active_carts.sql) and `044_sale_lines_overridden_price.sql`
   (never added to ALL array, never applied to any database).

4. Fix duplicated WAL comment in `run()` and update doc comment to
   mention foreign_keys PRAGMA.

All 17 migration tests pass. Clippy clean.
