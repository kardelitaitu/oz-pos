[project]
name = "prime-agent-skill-codebase-memory-mcp"
version = "0.1.0"
description = "Codebase Memory MCP integration skill for Prime Agent (DeusData/codebase-memory-mcp)."
requires-python = ">=3.10"
dependencies = ["mcp", "prime-agent-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/codebase_memory_mcp"]
