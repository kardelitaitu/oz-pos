feat: topology editor polish — wire dots, port alignment, delete fix, license tier, arrow fix

NodeTopologyEditor.tsx:
- PORT_OFFSET: Added 6px outward offset to align wire endpoints with port
  socket circle centers (was connecting to card edge, leaving ~6px gap)
- arrow-start marker: Changed from left-pointing to same right-pointing
  path as arrow-end. auto-start-reverse at path start now correctly
  flips it to point backward (two-way arrows now face correct direction)
- Delete fix: Nodes without wires now delete immediately instead of
  showing the confirm dialog with wrong title ("Delete Wire" for a node)
- Removed port-label span (dead code after removing CSS overlays)

NodeTopologyEditor.css:
- Wire style: Changed from solid to dotted (stroke-dasharray: 1 8 +
  stroke-linecap: round). Selected wires only change color, not style
- Port sockets: Behind card by default (z-index: -1), brought to front
  on hover (z-index: 1) for easy clicking
- Card background: Changed to --color-bg-elevated for solid appearance
- Removed port-label overlay CSS (top/right/bottom/left text on hover)

SettingsPage.tsx / MultiStoreDashboardScreen.tsx:
- Fixed license tier being hardcoded as 'standard' — now fetches
  actual tier via checkLicenseStatus() and normalizes to lowercase
  (e.g., 'Pro' -> 'pro') before passing to NodeTopologyEditor
