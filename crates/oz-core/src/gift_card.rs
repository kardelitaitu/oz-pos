//! Gift card domain types — re-exported from `modules_loyalty`.

pub use modules_loyalty::models::{
    GiftCard, GiftCardFilter, GiftCardTransaction, GiftCardWithTransactions, IssueGiftCardInput,
    RedeemGiftCardResult,
};
