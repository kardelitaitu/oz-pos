# oz-lua

Embedded Lua scripting runtime for OZ-POS (planned).

## Status

Scaffold only — `LuaError` defined. The `rlua`/`mlua` binding surface lands in a follow-up once `oz-core` and the cart state machine are stable.

> last audited 28-06-26 by docs-auditor
