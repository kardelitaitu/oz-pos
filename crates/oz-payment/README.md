# oz-payment

Payment processor abstraction for OZ-POS (planned).

## Status

Scaffold only — `PaymentError` defined. The `PaymentProcessor` trait and adapters (Stripe, Square, EMV terminal) land in a follow-up.

> last audited 28-06-26 by docs-auditor
