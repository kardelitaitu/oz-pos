#!/bin/sh
set -e

# Prompt to ask about preserving data.
if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
    echo "Note: database and config files in /var/lib/oz-pos are preserved."
    echo "To fully remove all data, delete /var/lib/oz-pos manually."
fi

exit 0
